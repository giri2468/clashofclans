# UI Design System

The web client uses Tailwind CSS and shadcn/ui primitives. The initial visual language is dark, high-contrast, responsive, and keyboard accessible.

Required UI states for each remote action: loading, success, empty, validation error, authorization error, and provider error. Form controls have visible labels, focus rings, and descriptive inline errors. Recommendation priority is communicated by text and colour, never colour alone.
